test compile
target x86_64

function %f0(i32) -> i32 {
    sig0 = (i32) -> f32
    fn0 = %g(i32) -> f32

    block0(v1: i32):
        v2 = f64const 0x1.0
        try_call fn0(v1), sig0, block1(v2), [ tag1: block2, default: block3(v2) ]

    block1(v3: f32, v4: f64):
        v5 = iconst.i32 1
        return v5

    block2(v6: i64):
        v7 = iconst.i32 2
        return v7

    block3(v8: i64, v9: f64):
        v10 = iconst.i32 3
        return v10
}
